import * as React from "react";
import { ProviderDetails } from "../hoc";

const DetailsPage: React.FC<React.ReactNode> = () => {
  return <ProviderDetails />;
};

DetailsPage.displayName = "Details Page";

export default DetailsPage;
