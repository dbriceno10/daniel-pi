import * as React from "react";
import NotFoundPage from "../container";

const NotFoundPagePage: React.FC<React.ReactNode> = () => {
  return <NotFoundPage />;
};

NotFoundPagePage.displayName = "Not Found Page";

export default NotFoundPagePage;
